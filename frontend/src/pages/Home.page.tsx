import React from 'react';
import { Container, Title } from '@mantine/core';

const HomePage: React.FC = () => {
    return (
        <Container>
            <Title className="text-2xl font-bold">Home Page</Title>
        </Container>
    );
};

export default HomePage;
